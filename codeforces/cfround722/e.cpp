#include <bits/stdc++.h>
using namespace std;

void solve();

int main() {
	ios_base::sync_with_stdio(false); 
	cin.tie(NULL);
	int t_;
	t_ = 1;
	// cin >> t_;
	for (int t__ = 0; t__ < t_; t__++) {
		solve();
	}

	return 0;
}

void solve() {
	int n, m;
	cin >> n >> m;

	for (int t = 0; t < m; t++) {
		int type, i, j;
		cin >> type >> i >> j;
	}

}
